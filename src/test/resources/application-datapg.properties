# profile: datapg
spring.h2.console.enabled=false
spring.datasource.url=jdbc:postgresql://localhost:5433/dbmoviedata
spring.datasource.username=movie
spring.datasource.password=password
# DDL generate:
# - none (production),
# - create (ie drop-create),
# - create-drop (ie drop-create - drop)
# - update (incremental DDL)
spring.jpa.hibernate.ddl-auto=none